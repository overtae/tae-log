on:
  push:
    branches:
      - gh-pages
name: Deploy Gatsby
jobs:
  build_gatsby:
    name: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1

      - name: npm install
        run: npm install

      - name: gatsby build
        env:
          GH_API_KEY: ${{ secrets.ACESS_TOKEN }}
        run: gatsby build

      - name: deploy
        uses: maxheld83/ghpages@v0.2.1
        env:
          GITHUB_TOKEN: ${{ secrets.ACESS_TOKEN }}
          GH_PAT: ${{ secrets.ACESS_TOKEN }}
          BUILD_DIR: 'public/'
